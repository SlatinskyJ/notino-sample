import styled from 'styled-components';

export const AppContent = styled.div`
  margin-top: 100px;
  width: 100%;
  height: auto;
  display: inline-flex;
  justify-content: center;
  position: fixed;
  overflow-y: auto;
  inset: 0;
`;